{
  "name": "@repo/shared",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "pnpm typecheck",
    "test": "pnpm verify",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "pnpm typecheck"
  }
}
